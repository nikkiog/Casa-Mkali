# CM Secure Assistant — Casa Mkali

# Slack Bot Configuration
# Create a Slack App at https://api.slack.com/apps
# Enable Socket Mode and add the required scopes:
#   channels:history, channels:read, groups:history, groups:read,
#   im:history, im:read, chat:write
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token

# Anthropic API
ANTHROPIC_API_KEY=sk-ant-your-api-key

# Fathom API (optional — enables meeting recap/transcript indexing)
# Get your API key from https://fathom.video/settings/api
FATHOM_API_KEY=

# Storage (optional — defaults to data/casa_mkali.db)
DB_PATH=data/casa_mkali.db
